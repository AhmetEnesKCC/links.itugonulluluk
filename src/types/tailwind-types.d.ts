export interface Colors {
  background: string;
  foreground: string;
  card: Card;
  popover: Card;
  primary: Card;
  secondary: Card;
  muted: Card;
  accent: Card;
  destructive: Card;
  border: string;
  input: string;
  ring: string;
  chart: Chart;
}
